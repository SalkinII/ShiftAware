In unified profile card:
Can we finally add it to the canvas side panel? This still only shows the avatar and the name. not the actual profile card. We want to reuse the same component everywhere!


If I create a shift completely anew there is still that Stationary mobile stuff. and no template selection option. I would like to select the shift. Can we harmonise this?

The event specific shifts are not properly wired into our canvas logic. they appear in the unassigned lane. We want to have complete consistency of behaviour.

<div class="bg-white rounded-2xl shadow-2xl p-8 max-w-xs w-full"><div class="flex flex-col items-center gap-3 mb-6"><div class="w-20 h-20 rounded-2xl bg-gray-50 flex items-center justify-center text-5xl shadow-inner border border-gray-100">🦫</div><h2 class="text-2xl font-black text-gray-900">Beaver</h2></div><p class="text-xs text-gray-400 text-center mt-6">Click outside or press Esc to close</p></div>
this still doesn't show the so called unified profile card. It just shows the name and the avatar. This is in the side bar to the right that hovers over the canvas when you click the avatar there.

