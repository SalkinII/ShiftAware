1. **User Profile attribute naming** — The read-only user profile shows the database attribute name instead of the human-readable name. Must be consistently human-readable everywhere, including in all business logic.

2. **Multi-select attribute logic** — No mechanism exists to allow several attributes ("one of") for multi-select attributes or broader logic when assigning shifts.

3. **Seed revamp incomplete** — The seed revamp was never fully run; the live database still reflects the old pre-revamp state.

4. **Console error on admin area navigation** — An empty/spurious console error fires when switching from Manage Team to Calendar in the admin area.

5. **Drag and drop broken on canvas** — Shift nodes cannot be picked up and dropped on the canvas. Resize handles still work. Nodes can be stretched across multiple lanes via resize but snap back to their original lane on drop — that snap is also an error. (Template drag-and-drop is unaffected.)

6. **Canvas sidebar shows shift name three times** — The right-side canvas sidebar displays the shift name three times. Only the largest card instance should remain; the duplicate occurrences must be removed.

7. **Inconsistent shift info retrieval across views** — The shift card info shown in the list view (user and admin left-side cards) must be identical to the canvas detail panel (right side) and the "create shift" side panel. A single coherent retrieval logic must cover all three surfaces.

8. **UI-1: Admin view toggle button jumps laterally** — Switching between list and calendar view causes the toggle button to shift position because the export button (calendar-only) is absent in list mode with no reserved space. Fix: use an invisible placeholder or restructure the toolbar so toggle position is independent of surrounding buttons. File: `app/admin/shifts/schedule/page.tsx` (toolbar area).

9. **UI-2: Admin schedule list view shows raw ShiftType enum** — List view cards display `STATIONARY` / `MOBILE_TEAM` etc. instead of the human-readable template name. Fix: add `template` field to the local `Shift` interface, fetch the template relation, and replace the raw enum fallback at line 843 with `shift.template?.name`. Files: `app/admin/shifts/schedule/page.tsx:53-66, 843`.

10. **UI-3: "Define New Shift" button non-functional in planning mode** — Clicking the button in planning mode does nothing; no panel opens. Root cause not yet investigated — likely a missing mode guard or incorrect action binding. File: `app/admin/shifts/schedule/page.tsx`.

11. **FEAT-1: Lock lane reorder after planning phase** — Lane order must become immutable once the event moves past PLANNING status, consistent with other locked settings. Fix: disable lane reorder controls when `event.status !== 'PLANNING'`. Files: `components/features/LaneCalendar/LaneCalendarCanvas.tsx` and lane reorder UI.

12. **FEAT-2: Shift node content layout redesign** — Shift block nodes need a new layout: Row 1 — name (left) + time (right). Row 2 — desirability, vote up/down, coverage count (colour-coded red when understaffed, green when fully staffed). Row 3 — unchanged. File: `components/features/LaneCalendar/nodes/ShiftBlockNode.tsx`.

13. **TypeScript warnings in test files** — Four test files use Vitest globals (`describe`, `it`, `expect`) without importing them; `tsc` does not recognise them. Fix: add `"types": ["vitest/globals"]` to `tsconfig.json`, or add explicit imports to each file. Affected files:
    - `tests/unit/DesirabilityBadge.test.tsx`
    - `tests/unit/laneReorder.test.ts`
    - `tests/unit/ProfileDetailCard.test.tsx`
    - `tests/unit/services/assignments.service.test.ts`

14. **Assignment preview UI — configured logic lost after run** — After the assignment algorithm runs, the previously configured logic/settings are lost in the preview UI.

15. **Assignment preview UI — resting period constraint** — Resting period constraint is not enforced or reflected in the assignment preview.

16. **Assignment algorithm tests and transparency** — No automated tests for the assignment algorithm; algorithm behaviour is opaque to users and developers.

17. **Attribute/skill validation during assignment** — Attribute and skill requirements are not validated when performing assignments.

18. **Zero-occupancy marker verification** — The zero-occupancy markers have not been verified as working correctly.

19. **Gender equality / attribute balance constraint** — Partially implemented (via `fa8a232`); must be completed to a fully working state.

20. **Canvas viewport reset on shift drop or PNG export** — The canvas viewport resets unexpectedly when a shift is dropped or when a PNG export is triggered. Root cause must be identified and definitively fixed — no deferral.
