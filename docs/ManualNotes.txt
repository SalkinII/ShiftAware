### Addressed (2026-02-17 bugfix pass)

- Preference polling: 30s auto-refresh on user calendar
- My Shifts empty: diagnostic logging + identity guard + user hint
- Compact zoom: capacity and member names visible at 2-day overview
- Day/Week/Grid toggle: removed (obsolete with React Flow canvas)
- PNG export: time ruler and lane labels now included
- Audit logging: preferences, swap-requests, config, registrations routes

### Remaining / to review

- Assignment icons (if still desired)
- Flow implementation overview
- Export: PDF for preference list kept; PNG lane view as option
