# Session Handoff: v1.4 Critical Fixes & Architecture Review

**Date:** 2026-01-18  
**Branch:** `iteration/v1.4-critical-fixes`  
**Source:** Playwright recording with user annotations (`test-results/playwrightclickthrough.txt`)

---

## Handoff

```yaml
from: @reviewer (user testing with Playwright)
to: @orchestrator → @implementer

context: |
  User performed interactive testing with Playwright codegen and annotated
  15+ issues ranging from database inconsistencies to UX problems.
  Screenshots provided: heatmap spacing, calendar lane width.
  
task: |
  Systematic analysis and fix plan:
  1. Database schema consistency (events appearing/disappearing)
  2. Form input behavior (text fields kicking user out)
  3. API validation errors (Prisma errors on create)
  4. View separation (user shouldn't access admin features)
  5. Coverage calculation bugs
  6. UI polish (heatmap, calendar, sticky headers)
  7. Code cleanup (remove over-engineered features)

blockers: 
  - Need to reproduce Prisma validation error
  - Database state inspection needed
```

---

## Issue Analysis

### P0: Critical Bugs

| # | Issue | Location | Evidence | Root Cause Hypothesis |
|---|-------|----------|----------|----------------------|
| 1 | Event creation fails | `/admin/festival/setup` | "PrismaClientValidationError" (line 44) | Missing required field or type mismatch in POST `/api/events` |
| 2 | "Starlight Meadow" appears but "No Events Yet" | Sidebar vs Festival Setup | Lines 16, 18 | Seeded data vs API query mismatch (sidebar uses different endpoint?) |
| 3 | Text inputs kick user out | Create Event form | Lines 19-27, 41 | Likely controlled input with parent re-render or form state reset |

### P1: Data Integrity / Logic Bugs

| # | Issue | Location | Evidence | Fix |
|---|-------|----------|----------|-----|
| 4 | Deactivating members doesn't affect coverage | Coverage page | Lines 60, 85 | Filter inactive members from coverage calculation |
| 5 | Coverage always shows 100% | Coverage cards | Line 60 | Bug in `gaps` calculation - check `isActive` filter |

### P2: View Separation / RBAC

| # | Issue | Location | Current | Desired |
|---|-------|----------|---------|---------|
| 6 | Templates in user calendar | `/app/calendar` | TemplatePalette visible | Hide from non-admin users |
| 7 | Users can drag-drop shifts | `/app/calendar` | DnD enabled for all | Disable DnD for non-admin |
| 8 | Shift Schedule page identical | `/app/calendar` vs `/admin/shifts/schedule` | Same code | Separate concerns |

### P3: Algorithm & Dashboard Transparency

| # | Issue | Description | Action |
|---|-------|-------------|--------|
| 9 | Algorithm Power panel misleading | Shows on dashboard but algo unclear | Hide if not configured, or link to setup |
| 10 | Algorithm location unknown | User asks "where does it live?" | Document in UI, add "Configure Algorithm" link |
| 11 | Dashboard scores unexplained | Numbers shown but meaningless | Either explain or remove |

### P4: UI/UX Polish

| # | Issue | Screenshot | Current | Desired |
|---|-------|------------|---------|---------|
| 12 | Heatmap too spaced | `grid to spaced out.png` | Large cells, wasted space | Smaller cells, denser grid |
| 13 | Calendar lanes too narrow | `calenderlaneswider.png` | Shift bars start-aligned | Bars span actual duration proportionally |
| 14 | Sticky header doesn't work | Line 53 | CSS sticky applied | Actually sticky during scroll |
| 15 | Tooltip uninformative | Line 55 | Just "Partial" | Show conflict reason, preferences, availability |

### P5: Code Cleanup

| # | Issue | Location | Action |
|---|-------|----------|--------|
| 16 | ConflictWizard over-engineered | `ConflictWizard.tsx` | Line 88: "we can scrap that" | Remove or radically simplify |
| 17 | "View Schedule" button redundant | Dashboard | Line 47: "not needed, can click sidebar" | Remove |

### P6: Missing Features (Lower Priority)

| # | Feature | Current | Desired |
|---|---------|---------|---------|
| 18 | Publish workflow | "Coming soon" | 2-stage publish (voting open → schedule published) |
| 19 | Profile page | "Coming soon" | User selects avatar identity, sees their assignments |
| 20 | Gender options | "M_NB" unclear | FLINTA, NB, Male, or custom |

---

## Investigation Steps

### Step 1: Database State Analysis

```sql
-- Check events in database
SELECT id, name, status, "startDate", "endDate" FROM "Event";

-- Check if sidebar is reading from different source
-- Hypothesis: Sidebar uses useCurrentEvent which calls /api/events/current
-- Festival Setup uses /api/events (GET all)

-- Check event config
SELECT e.name, ec.* FROM "Event" e LEFT JOIN "EventConfig" ec ON e.id = ec."eventId";
```

### Step 2: Reproduce Prisma Validation Error

1. Clear browser storage/cookies
2. Login as admin
3. Navigate to Festival Setup
4. Attempt to create new event
5. Capture full error in Network tab

### Step 3: Text Input Bug Reproduction

1. Open Festival Setup → Create Event form
2. Type in "Event Name" field
3. Observe if cursor jumps / input loses focus
4. Check React DevTools for re-renders

---

## Implementation Plan

### Phase 1: Critical Data Fixes (P0)

```
1. [ ] Fix Prisma validation error in POST /api/events
       - Check schema matches API payload
       - Add better error logging
       
2. [ ] Investigate sidebar vs festival setup event discrepancy
       - Trace data flow: useCurrentEvent → /api/events/current
       - Trace data flow: Festival Setup → /api/events
       
3. [ ] Fix text input losing focus
       - Check if CreateEventForm is defined inside component (causes remount)
       - Move to separate component or use useCallback for handlers
```

### Phase 2: Logic/Calculation Fixes (P1)

```
4. [ ] Fix coverage calculation to exclude inactive members
       - In /api/shifts response, filter by isActive
       - In Coverage page, recheck gaps calculation
       
5. [ ] Ensure member deactivation reflects immediately
       - Invalidate cache on member status change
```

### Phase 3: View Separation (P2)

```
6. [ ] Split /app/calendar (user view) from /admin/shifts/schedule
       - User view: Read-only calendar, no DnD
       - Admin view: Full editing, templates, DnD
       
7. [ ] Remove TemplatePalette from user calendar
       - Conditional render based on isAdmin
```

### Phase 4: UI Polish (P4)

```
8. [ ] Condense heatmap further
       - Review screenshot: cells still too large
       - Target: 24px cells, minimal padding
       
9. [ ] Fix calendar lane width
       - Shift bars should span proportional to duration
       - 8-hour shift = ~33% of 24h row width
       
10. [ ] Fix sticky header
        - Review CSS: may need position: sticky on correct parent
        - Check z-index layering
```

### Phase 5: Code Cleanup (P5)

```
11. [ ] Simplify or remove ConflictWizard
        - If keeping: Make it inline suggestions, not modal wizard
        - If removing: Delete component, update imports
        
12. [ ] Remove redundant "View Schedule" button
        - Dashboard cleanup
```

### Phase 6: Transparency & UX (P3)

```
13. [ ] Make Algorithm Power panel conditional
        - Show only if algorithm has been run
        - Or link to setup/explanation
        
14. [ ] Improve heatmap tooltips
        - Show: member name, shift details, preference if any, conflict reason
```

---

## Files Affected

```
# P0: Critical
app/admin/festival/setup/page.tsx     # CreateEventForm input bug
app/api/events/route.ts               # Prisma validation error
lib/hooks/useCurrentEvent.ts          # Event data source

# P1: Logic
app/admin/coverage/page.tsx           # Coverage calculation
app/api/shifts/route.ts               # Filter inactive members
app/api/members/route.ts              # Include isActive in response

# P2: View separation
app/app/calendar/page.tsx             # Remove admin features
middleware.ts                         # RBAC checks

# P4: UI
components/features/AvailabilityHeatmap/AvailabilityHeatmap.tsx
components/features/Calendar/CalendarView.tsx
components/features/Calendar/CalendarView.css

# P5: Cleanup
components/features/ConflictWizard/ConflictWizard.tsx  # Simplify/remove
app/app/dashboard/page.tsx            # Remove redundant button
```

---

## Testing Checklist

- [ ] Create new event from scratch (no seed data)
- [ ] Text input fields maintain focus
- [ ] Deactivate member → coverage recalculates
- [ ] User view shows read-only calendar
- [ ] Admin view shows editable calendar with templates
- [ ] Heatmap renders densely (screenshot comparison)
- [ ] Calendar shifts span proportional width
- [ ] Sticky header stays fixed during scroll

---

## Screenshots Reference

1. **Heatmap spacing**: `test-results/grid to spaced out.png`
   - Issue: Cells too large, too much whitespace
   - Target: Dense, compact grid

2. **Calendar lanes**: `test-results/calenderlaneswider.png`
   - Issue: Shift bars are small, start-aligned
   - Target: Bars span proportional to duration (8h = wider bar)

---

## Next Steps

1. **@orchestrator**: Triage this plan with user
2. **@implementer**: Start with P0 (database/form issues)
3. **@reviewer**: Validate fixes with Playwright re-run

