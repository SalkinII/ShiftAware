ShiftAware Backlog
==================

Ref: docs/plans/2026-02-26-backlog-list-plan.md

## Phase 1 — Canvas Bug Fixes

- Shift resize left handle — empty toast, wrong position (ignore node.position.x)
- Canvas full reload on drag-drop — merge strategy, fix fitView trigger
- Node content stacked — grid layout for DetailedContent

## Phase 2 — Canvas UX

- Lane names — initials (multi-word) or first 3 letters (single)
- Time ruler dates — add year (dd.MM.yyyy)
- Contextual zoom — useStore summary/detail by zoom level
- Colour harmonization — templates match lanes

## Phase 3 — Export & Markers

- Export — choose React Flow PNG vs PDF
- Zero-occupancy shifts as markers

## Phase 4 — User View

- User list view — want/don't want, assignment state (reuse admin logic)
- Password-based event selection in user view

## Phase 5 — Admin Parity

- Admin team — same detail/create logic as identity page
- Profile card read-only on avatar click (user + admin)

## Phase 6 — Service

- Attribute/skill validation during assignment

## Phase 7 — Audit

- Full UI audit — DESIGN.md & ARCHITECTURE.md compliance (gap report only)

---

## Deferred (not in plan)

- Full UI consistency audit — inventory shift display views (list, canvas, panel) and harmonize. Ref: REQUIREMENTS.md 8.1-8.2.

- Window-switching empty calendar bug — monitor after cache invalidation. Ref: REQUIREMENTS.md 7.4.

- RegistrationStatus workflow — REGISTERED → CONFIRMED → DECLINED. Schema exists, no service logic. Ref: brainstorming 2026-02-26.
