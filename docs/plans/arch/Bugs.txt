# ShiftAware — Bug and Issue Register

---


## Fixed — v3.11 Stabilization (2026-02-28)

All fixes committed to branch `v3.11`. Tests: 305/305 pass. Production TS errors: 0.

| Bug | Description | Commit |
|-----|-------------|--------|
| #12 | FK constraint `SwapRequest_fromAssignmentId_fkey` — delete SwapRequests before Assignments | `ef6efe6` |
| #3  | DnD coordinate calculation after lane reorder — pass `orderedLanes` to `useCanvasActions` | `baad209` |
| #7  | Require ratio default upper bound reads 10000 — `maxRatio ?? 1` | `1ef71a7` |
| #6  | Zero-occupancy minimum 1 enforced — `min="0"`, remove `|| 1` coercions | `1ef71a7` |
| #9  | "Define New Shift" → "Cancel" button jump — `min-w-[11rem]` + X icon | `1ef71a7` |
| #11 | Preview results header square corners — `rounded-t-2xl` | `1ef71a7` |
| #1  | Template selector in assignment rules jumps / non-unique values — use `t.id` not `t.type` | `054f400` |
| #2  | Sidebar card shows "Shift" — add `template` include to shift repository + user calendar | `cab530f` |
| #8  | PNG export viewport jumps to far right — save/restore viewport around capture | `911efb6` |
| #10 | Planning → Preferences card layout jump — `min-h-[700px]` on content container | `776a393` |
| #5  | Buffer days stale input — full 3-layer removal (schema, validation, UI, calendar) | `b0045e9` |
| #13 | Seed missing `templateId` links and stale config keys — full rewrite | `cc72e51` |

---

## Fixed — earlier versions

| Bug | Description | Version |
|-----|-------------|---------|
| Lane reorder: shifts move independently of lanes | Merged `mergeNodes` fix | v3.5b |
| Template palette: colour stripes causing confusion | Removed colours, added grip handle | v3.5b |
| Distribution rules not persisted on save | `allocationRules` persisted via Prisma + Zod + route | v3.5b |
| ProfileDetailCard: avatar click showed useless info | Full member info with attributes on admin team page | v3.5b / v3.7 |
| User calendar: dead staffed/partially-staffed cards | Removed from Team Calendar view | v3.6 |
| ShiftPropertiesPanel: shows raw Prisma enum | Template name fallback added | v3.5b |
| CreateProfileForm reuse in admin team page | Verified working | v3.5b |
| Attribute rules: no template import in team management | shiftType dropdown loads from event templates | v3.7 |
| Members lack event attributes on new event | Attribute prompt reused from Assume Identity | v3.7 |
| Canvas view consistency | Unified canvas patterns | v3.6 |
| Desirability colour mismatch (yellow vs grey) | Unified badge + hotfix | v3.6 |
| User calendar role filter inappropriate | Replaced with attribute filter | v3.6 |
| User calendar filter/counter layout | Adjusted layout per design | v3.6 |
| PNG export missing (PDF only) | PNG export added | v3.6 / v3.11 |
| Lane reorder feature | Up/down buttons + lock | v3.6 |
| Identity/event selection auto-forward | Escape affordance added | v3.6 |

---

## Out of scope — future phases

- **v3.8:** Assignment preview UI, configured logic lost after run, resting period constraint, algorithm tests and transparency
- **v3.8:** Attribute/skill validation during assignment
- **v3.9:** User list view (preferences + assigned shifts), password-based event selection, export choice (PNG vs PDF table), zero-occupancy marker verification
- **v3.10:** Full UI audit, documentation compliance gap report
- **General:** Gender equality / attribute balance constraint (partially implemented via `fa8a232`)
- **General:** Canvas viewport reset on shift drop — root cause documented in `docs/plans/arch/`, fix deferred
- **General:** Template reservoir colour schema
