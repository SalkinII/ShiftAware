- Plan multi event Flow and cross event booking - member in different events will cross block time slots for other event if shift in first event is already booked.
  → DONE (2026-09-01): a member already assigned to an overlapping/too-close
    shift in another event is now blocked from double-booking. Added
    `conflictingShiftId` to `ConstraintViolation`, a distinct `"cross_event_conflict"`
    reason (separate from same-event `"time_conflict"`) throughout
    `lib/algorithm/{types,validator,can-assign}.ts`, and a pure
    `seedCrossEventConflicts` helper (`lib/algorithm/cross-event-conflicts.ts`)
    that seeds a member's cross-event bookings into the algorithm's own
    `runAssignmentAlgorithm` (via `lib/domain/allocation.ts`) and into the
    distribution heatmap's client-side `canAssign` preview
    (`DistributionHeatmap.tsx`), so admins see the same blocked cells the
    algorithm itself will respect. Verified live: a cross-event-conflicted
    cell renders blocked with the correct label, the existing
    manual-override `confirm()` flow still works, and a dry-run algorithm
    pass never assigns into a cross-event-conflicting shift.
    Spec: docs/superpowers/specs/2026-08-31-multi-event-cross-booking-design.md

- Extend attributes for time constraint (will only arrive by - cannot work 22:00 to 06:00 - will need to leave on date - time - setting gard gates for planning)
  → DONE (2026-09-01): added a `TIME_CONSTRAINT` team-attribute type backed
    by a pure `evaluateTimeConstraint` function (`lib/algorithm/time-constraint.ts`)
    supporting per-member arrival/departure availability windows and
    repeating daily blackout hours (including midnight-wrapping ranges).
    Wired as a new hard gate in `canAssign` (checked after FILTER rules),
    with real attribute names sourced from each event's
    `EventAttributeDefinition`s in both the algorithm and the heatmap.
    Added a shared `AttributeValueField` component
    (components/features/Identity/AttributeValueField.tsx) with a
    dedicated editor (add/remove availability windows and blackouts) and a
    human-readable read-only display, consolidating what was previously
    duplicated inline attribute-editing JSX across
    `AttributePromptModal.tsx`, `CreateProfileForm.tsx`,
    `MemberListByEvent.tsx`, and `ProfileDetailCard.tsx`. Verified live: a
    blackout set via the real editor UI renders as readable text (not raw
    JSON) on the profile card, blocks the matching heatmap cell with the
    correct label, and a cross-event conflict and a blackout on the same
    member render with distinct, correctly-labeled reasons on different
    cells. A dry-run algorithm pass never assigns a member into their own
    blacked-out shift.
    Spec: docs/superpowers/specs/2026-08-31-time-constraint-attribute-design.md

- Swap banner not to overlay date display field in canvas when on mobile.
  → DONE (2026-08-31): the actual culprit was the "⇄ N swaps pending" mobile
    badge in app/admin/shifts/schedule/page.tsx, absolutely positioned
    top-3/right-3 relative to the page-level canvas container. Root cause:
    that offset assumed the ruler starts at the container's own top, which
    broke whenever the shiftMutationLocked banner (a separate, variable-
    height element) was also showing above it — confirmed live in a browser
    at a 390x844 viewport with both active simultaneously, where the badge
    still covered the "03:00" ruler label after a first attempt that only
    accounted for the ruler.
    Along the way also fixed a second, real overlap bug in the same
    component: the shiftMutationLocked lock bar itself was `position:
    absolute` with a hardcoded `paddingTop: 36` reservation, so on mobile
    the message wrapped to two lines and its own overflow covered the ruler.
    Fixed both by removing the magic-number layout entirely: the lock bar is
    now a normal flex-column child (LaneCalendarCanvas.tsx), and the swap
    badge moved from the page into a new `topRightOverlay` prop on
    LaneCalendarCanvas, anchored inside its flow-container div (which always
    starts exactly at the ruler, regardless of the lock banner's presence or
    height). Verified live: lock banner + "2 swaps pending" badge shown
    together, ruler fully readable, no overlap.

- Bespoke lane - 0 person shifts with editable markers for comments and other time table aspects to write into the shift plan. e.g. replacing "The unassigned" lane.
  → DONE (2026-09-01): the "Unassigned" catch-all lane is now labeled
    "Notes" and hosts free-text plan markers backed by a new `PlanMarker`
    model, `MarkerRepository`, and `/api/markers` CRUD routes. On the
    canvas, markers render as their own draggable/resizable React Flow node
    type (`MarkerNode`), created by dragging a new "📝 Add Note" pill
    (`AddMarkerPill.tsx`) onto the Notes lane, edited inline with
    click-to-type and blur/Enter-to-save, resized via the same
    `NodeResizer`+`handleResizeEnd` pattern as shift blocks, and deleted via
    a `confirm()`-gated `×` button matching the existing shift-delete
    pattern. The schedule page gets full CRUD (`AddMarkerPill` +
    read/write); the read-only user calendar renders markers visible but
    non-interactive. Markers are deliberately invisible to the allocation
    algorithm, the distribution heatmap, and the analysis table — confirmed
    live. Two real gaps were found and fixed only through live
    browser testing (not caught by unit tests alone): `MarkerNode` never
    rendered a `NodeResizer` at all, and even after adding it, nothing
    wired `NodeResizer`'s `onResizeEnd` through to the node's data — both
    are now connected end-to-end and verified with an actual drag-resize
    that persists.
    Spec: docs/superpowers/specs/2026-08-31-bespoke-marker-lane-design.md

- Ui canonicals accross all pages and elements.
  → DONE (2026-09-01): fixed the real root cause the original UI survey
    misdiagnosed as a "config mismatch" — Tailwind v4 never loaded
    tailwind.config.ts (no `@config` directive anywhere in the repo, per
    Tailwind v4 docs), so every `error-*` class (used throughout `Input`,
    the destructive `Button` variant, delete icons, etc.) rendered nothing
    at all. Deleted the dead config file, added a real `--color-error-*`
    scale to `app/globals.css`'s `@theme` block, and fixed the destructive
    `Button` variant's now-invisible-on-red `text-red-600` to `text-white`.
    Extracted a `Pill` primitive (components/ui/Pill.tsx) shared by
    `StatusBadge` and the schedule page's swap-pending badge. Converted 5
    event-wide/multi-record `confirm()` call sites (replace assignments,
    run algorithm, delete attribute, remove member, event status
    transition) to the existing `ConfirmDialog` component, per the spec's
    rule distinguishing those from single-object reversible actions (left
    unchanged). Surveyed the whole codebase for hand-rolled Card-shaped
    wrappers duplicating `<Card>`'s classes; found one genuine violation
    (`SkeletonCard` using a different radius/border/padding than the real
    `<Card>` it stands in for) and converted it. Verified live across
    /admin/setup, /admin/shifts/schedule, /admin/events/[id]/distribution,
    /admin/team, and /app/calendar: real red colors render everywhere
    error-* is used, every converted confirm() now opens a ConfirmDialog
    instead of the native browser dialog, and no unrelated pages regressed.
    Spec: docs/superpowers/specs/2026-08-31-ui-canonicalization-design.md
