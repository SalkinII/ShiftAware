Manual Bugs and Notes - Status after 2026-02-11 Bugfix Sweep
============================================================

RESOLVED (2026-02-11):
#1: Event selection on identity page - FIXED (Card onClick prop added)
#2: Event creation fails - FIXED (minExperienceMix: true → 1, z.record(z.number()) compliance)
#3: Gender balance detection dead - FIXED (memberAttributesMap passed to validateGenderBalance)
#4: priority → wantLevel migration incomplete - FIXED (audit rollback, optimizer, scorer, shift repo)
#5: Attribute creation duplicate gives generic error - FIXED (409 with clear message)
#6: EventSelector onSelect null type mismatch - FIXED (onSelect accepts string | null)

OUTSTANDING (pre-existing, not in sweep scope):
- Some test fixtures need schema updates (Event, ShiftTemplate, etc.)
- AttributeDefinitions.tsx: AttributeType narrowing
- Prisma.PreferenceLevel, Prisma.Role namespace exports (check Prisma client version)
