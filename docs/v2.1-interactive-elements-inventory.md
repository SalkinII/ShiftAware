# ShiftAware v2.1 Interactive Elements Inventory

**Purpose:** Technical reference mapping UI elements to code for v2.1 fixes.
**Format:** Add comments in the "Notes" column to guide implementation.

---

## LOGIN PAGE

**Route:** `/login`
**File:** `app/login/page.tsx`

| # | Element | Type | Code Reference | Notes |
|---|---------|------|----------------|-------|
| L1 | Password input | input[type=password] | `id="password"` | |
| L2 | Sign In button | button[type=submit] | `handleSubmit()` → POST `/api/auth/login` | |
| L3 | Error message | div | Conditional render on `error` state | |

---

## IDENTITY PAGE

**Route:** `/app/identity`
**File:** `app/app/identity/page.tsx`

### Member Selection View
// The Emojis are no longer there. We used to have them. The reserved ones (octopus for the app and bear for the admin are still showing. But the cards here don't have them); the Member 1234 (lead) senior is greyed out and not clickable. What's the logic there?
| # | Element | Type | Code Reference | Notes |
|---|---------|------|----------------|-------|
| I1 | Member card (each) | clickable div | `onSelectMember(member.id)` → localStorage + navigate | |
| I2 | Create New Profile button | button | `setShowCreateForm(true)` | |

### Create Profile Form

**File:** `app/app/identity/components/CreateProfileForm.tsx`
// The capabilities are something we developed anew. Later I will see how that checks out with the traits we set in the config view as admin. But I can already say that for example the gender is something that seems to be hard coded in the prisma schema but would probably need to be defined. I mean we can offer a variety of relevant traits that are typed for shift work. but they should be edittable and maintained in the database and then projected here for the people to be able to fill in if they need it. We can also leave traits open and just offer the pseudonyms as stubs so people can enter their own data for certain traits (e.g. gender in particular). The mandatory setting that you put into the config is quite relevant. we should be able to enforce that here.
| # | Element | Type | Code Reference | Notes |
|---|---------|------|----------------|-------|
| I3 | Display Name input | input[type=text] | `formData.alias` | |
| I4 | Experience Level dropdown | select | `formData.experienceLevel` | |
| I5 | Capabilities checkboxes | checkbox[] | `formData.capabilities` (SHIFT_LEAD, DRIVER, FIRST_AID) | |
| I6 | Create Profile button | button[type=submit] | POST `/api/members` → navigate | |
// once the profile was created it would be nice to also have a representation of the identity in the header. Also we could offer to go back to choosing identity. that way people could enter for each other.
---

//side note: If the browser window is very narrow, the side bar that is static is replaced by one that is collapsable. That one still shows the old routing. I want you to waterproof the routing in this iteration.

## USER CALENDAR PAGE

**Route:** `/app/calendar`
**File:** `app/app/calendar/page.tsx`

### Header Controls

| # | Element | Type | Code Reference | Notes |
|---|---------|------|----------------|-------|
| C1 | My Shifts toggle | button | `setCalendarView('my-shifts')` | |
| C2 | Full Schedule toggle | button | `setCalendarView('full-schedule')` | // This is still the old calendar widget. this one had scrolling and all types of things that are missing from the new one you made (drag and drop in setup). So we want to have the useful features to navigate.|
| C3 | View Type toggle (Day/Week/Grid) | button group | `setViewType()` - only in Full Schedule | //exactly - we wanted a horizontal scroll for the day and week view. We want to be able to pick a central date or time frame. all those features we talked about when implementing the new lane view. we want to use the new lane view (see later) but have all the features we had already. |
| C4 | Refresh button | button | `refetchShifts()` | |

### My Shifts View

**File:** `app/app/calendar/components/MyShiftsList.tsx`

| # | Element | Type | Code Reference | Notes |
|---|---------|------|----------------|-------|
| C5 | Shift card (each) | div | Display only | |
| C6 | Request Swap button | button | `onRequestSwap(assignment.id)` | |
| C7 | I Want This button | button | `onVoteWant(shift.id)` | |
| C8 | I Don't Want This button | button | `onVoteDontWant(shift.id)` | |

### Full Schedule View
// Coverage metrics is irrelevant for the user. I see that this might be useful in admin view. but not for the user.
| # | Element | Type | Code Reference | Notes |
|---|---------|------|----------------|-------|
| C9 | Coverage metrics cards | div[] | Display: coverage %, staffed, partial, unstaffed | |
| C10 | Coverage filter (All/Full/Partial/Empty) | button group | `setCoverageFilter()` | // this might be useful for the user but only if it allows for a condensed view or highlighting which shifts are still open.|
| C11 | Role filter dropdown | select | `setRoleFilter()` | |
| C12 | Member filter dropdown | select | `setMemberFilter()` | |
| C13 | CalendarView component | component | `components/features/Calendar/CalendarView.tsx` | // is this ne new lane view? Does the new lane view have all the features from the old calendar view? |
| C14 | Shift click → modal | click handler | `setSelectedShift(shift)` | |

### Shift Details Modal

| # | Element | Type | Code Reference | Notes |
|---|---------|------|----------------|-------|
| C15 | Close button (X) | button | `setSelectedShift(null)` | |
| C16 | Assignments list | div[] | Display only | |
| C17 | Close button (bottom) | button | `setSelectedShift(null)` | |

---

## EXPORT PAGE

**Route:** `/app/export`
**File:** `app/app/export/page.tsx`

| # | Element | Type | Code Reference | Notes |
|---|---------|------|----------------|-------|
| E1 | Export My Shifts button | button | `handleExportMyShifts()` → html2canvas |// ok this does not make any sense how this is set up. Either it works on a separate page or it it is a button. but either way this currently does nothing userful at all |
| E2 | Export Full Calendar button | button | `handleExportFullCalendar()` → html2canvas | // exactly the same it gives me a pop up that tells me to go to the calendar view. but there I can't fix anything |

---

## ADMIN SETUP PAGE

**Route:** `/admin/setup`
**File:** `app/admin/setup/page.tsx`

### Tab Navigation

| # | Element | Type | Code Reference | Notes |
|---|---------|------|----------------|-------|
| S1 | Event Settings tab | button | `setActiveTab('event')` | |
| S2 | Shift Templates tab | button | `setActiveTab('templates')` | |
| S3 | Team Attributes tab | button | `setActiveTab('attributes')` | |

### Event Settings Tab

**File:** `app/admin/setup/components/FestivalSettings.tsx`

| # | Element | Type | Code Reference | Notes |
|---|---------|------|----------------|-------|
| S4 | Event Name input | input[type=text] | | |
| S5 | Status dropdown | select | PLANNING, OPEN_FOR_PREFERENCES, ASSIGNING, FINALIZED | |
| S6 | Start Date input | input[type=date] | | |
| S7 | End Date input | input[type=date] | | |
| S8 | Buffer Days Before input | input[type=number] | | |
| S9 | Buffer Days After input | input[type=number] | | |
| S10 | Save Event Settings button | button | |// doesn't create a festival. Also flow is not clear because I would want to set up shifts templates and team attributes but if I switch the register card the data is lost. But also I can't save the event. I also can't see the events that are obviously already stored somewhere. We should make sensible use of our database and the existing middleware |

### Shift Templates Tab

**File:** `app/admin/setup/components/TemplateManager.tsx`

| # | Element | Type | Code Reference | Notes |
|---|---------|------|----------------|-------|
| S11 | New Template button | button | `setShowForm(true)` |// This shows: "Create Template Template creation form will go here" when clicked. you obviously skipped that. We want that properly wired too |
| S12 | Template edit button (each) | button | |// doesn't work |
| S13 | Template delete button (each) | button | |// doesn't work |

### Team Attributes Tab

**File:** `app/admin/setup/components/AttributeDefinitions.tsx`
// When creating a new attribute and entering the selections for select and multiselect, the last entry get's lost when save is hit. A new empty option has to be created to save that last one too. Also I would want to choose from all attributes that were already made for other events. For example you just have the can drive attribute. but where is the gender for example. Or did we wipe the database? Also I checked. If I make a new trait it does not get offered on the create new profile page at the start. so clearly there is some issue with the database wiring.
| # | Element | Type | Code Reference | Notes |
|---|---------|------|----------------|-------|
| S14 | Add Attribute button | button | | |
| S15 | Attribute name input | input | | |
| S16 | Attribute type dropdown | select | BOOLEAN, SELECT, MULTISELECT, TEXT | |
| S17 | Attribute options input | input | For SELECT/MULTISELECT | |
| S18 | Delete attribute button (each) | button | | |

---

## ADMIN SCHEDULE PAGE

**Route:** `/admin/shifts/schedule`
**File:** `app/admin/shifts/schedule/page.tsx`

### Header Actions

| # | Element | Type | Code Reference | Notes |
|---|---------|------|----------------|-------|
| SC1 | Publish Shifts button | button | Shows toast only (stub?) | |
| SC2 | Define New Shift / Cancel button | button | `setShowForm(!showForm)` | |

### Filter Controls

| # | Element | Type | Code Reference | Notes |
|---|---------|------|----------------|-------|
| SC3 | Event filter dropdown | select | `setSelectedEventId()` |// here I still get the old events from the db |
| SC4 | List view button | button | `setViewMode('list')` | |
| SC5 | Calendar view button | button | `setViewMode('calendar')` | |
| SC6 | View type toggle (Day/Week/Grid) | button group | `setViewType()` | |

### Shift Creation Form (sidebar)

| # | Element | Type | Code Reference | Notes |
|---|---------|------|----------------|-------|
| SC7 | Event Context dropdown | select | `formData.eventId` | |
| SC8 | Shift Type dropdown | select | MOBILE_TEAM_1, MOBILE_TEAM_2, STATIONARY, EXECUTIVE | |
| SC9 | Start DateTime picker | DateTimePicker | `formData.startTime` | |
| SC10 | End DateTime picker | DateTimePicker | `formData.endTime` | |
| SC11 | Priority dropdown | select | CORE, BUFFER | |
| SC12 | Score input (1-5) | input[type=number] | `formData.desirabilityScore` |// I still have never learnt what priority this is. You dreamt this up at some point. But what does it signify? Is 5 good or bad? |
| SC13 | Staff Capacity input | input[type=number] | `formData.capacity` | |
| SC14 | Register Shift Template button | button[type=submit] | `handleSubmit()` | |

### List View

| # | Element | Type | Code Reference | Notes |
|---|---------|------|----------------|-------|
| SC15 | Shift card (each) | div | Click expands |// that does not do anything that chevron to the right. It is stale |
| SC16 | Delete shift button (each) | button | `handleDeleteShift(id)` | |

### Calendar View (LaneCalendarView)

**File:** `components/features/LaneCalendar/LaneCalendarView.tsx`
// So in principle this is what I want. However: There are sensible scrolling features in the old calendar view that we have not implemented. The feature was meant to allow a flexible display of 1 to 7 days. We should have all that we want to display in one window. for example on the dates the last one is cut off. The shifts should have the display names as we specify in the templates. not just the trait. Again this is a backend issue. Finally we want this to be our main view that is displayed neatly in read only for the user. not have two calendar views.
| # | Element | Type | Code Reference | Notes |
|---|---------|------|----------------|-------|
| SC17 | Lane drop zone (each lane/day) | droppable | `data-testid="lane-drop-{date}-{type}"` | // so it only snaps to 00:00 |
| SC18 | Shift block (each) | draggable + clickable | `ShiftBlock.tsx` |// doesn't stay where it is dragged. Doesn't snapp to end of shift. If shifts are in succession they are staggered in the same lane. So no clear view. |
| SC19 | Shift resize handles | drag handles | `ResizeHandle.tsx` |// it just drags to new place. doesn't resize start/end |
| SC20 | Shift edit popover | popover | `ShiftEditPopover.tsx` |// is cut off by the window in which the calendar lives - so it is use less. It would make more sense to open it in the shift spec sidebar you have. Also if I enter another end date there is a toast error|
| SC21 | Time ruler (top) | component | `TimeRuler.tsx` |// not there |
| SC22 | Time ruler (bottom) | component | `TimeRuler.tsx` |// not there |
| SC23 | View mode controls | component | `ViewModeControls.tsx` | |

### Template Palette

**File:** `components/features/TemplatePalette/TemplatePalette.tsx`

| # | Element | Type | Code Reference | Notes |
|---|---------|------|----------------|-------|
| SC24 | Template card (each) | draggable | `handleDragStart` sets `activeTemplate` |// it doesn't map the time properly. It just drops into the day at a fixed time. it can't be moved after it is dropped |

### Delete Confirmation Dialog

| # | Element | Type | Code Reference | Notes |
|---|---------|------|----------------|-------|
| SC25 | Confirm Delete button | button | `confirmDelete()` | |
| SC26 | Cancel button | button | Closes dialog | |

---

## ADMIN TEAM PAGE

**Route:** `/admin/team`
**File:** `app/admin/team/page.tsx`

### Tab Navigation

| # | Element | Type | Code Reference | Notes |
|---|---------|------|----------------|-------|
| T1 | Team Members tab | button | `setActiveTab('members')` | |
| T2 | Allocation & Distribution tab | button | `setActiveTab('allocation')` | |

### Team Members Tab
// there are team members that are offered after login but there is no one present here. So also some issue with the db connectivity.
| # | Element | Type | Code Reference | Notes |
|---|---------|------|----------------|-------|
| T3 | Member list | component | Renders from `/admin/team/manage` | |
| T4 | Add member button | button | | |
| T5 | Edit member button (each) | button | | |
| T6 | Delete member button (each) | button | | |

### Allocation Tab

**File:** `app/admin/team/components/DistributionSettings.tsx`
// this throws a lot of errors as toast. What is the database concept? How are the settings handled? The rule spec is nice. But I would ask myself: I can't specify an event. there is the old database stuff presented to me and I can still select. So there is a serious issue with separation of concerns for different events. I think you need to review the connectivities.
| # | Element | Type | Code Reference | Notes |
|---|---------|------|----------------|-------|
| T7 | Fairness Weight slider | input[type=range] | 0-100, default 50 | |
| T8 | Preference Weight slider | input[type=range] | 0-100, default 30 | |
| T9 | Max Shifts Per Person input | input[type=number] | 1-50, default 12 | |
| T10 | Min Rest Hours input | input[type=number] | 0-24, default 8 | |
| T11 | Add Rule button | button | `handleAddRule()` | |
| T12 | Rule: Shift Type dropdown | select | Per rule row | |
| T13 | Rule: Attribute dropdown | select | Per rule row | |
| T14 | Rule: Operator dropdown | select | EQUALS, NOT_EQUALS, CONTAINS | |
| T15 | Rule: Value input | input | Per rule row | |
| T16 | Delete Rule button (each) | button | `handleDeleteRule(id)` | |
| T17 | Save Configuration button | button | `handleSave()` | |
| T18 | Preview Results button | button | `handlePreview()` |// this needs to be checked waterproof. That the hour calculations and all that stuff is rock solid. no unfairness! |

---

## ADMIN AUDIT PAGE

**Route:** `/admin/audit`
**File:** `app/admin/audit/page.tsx`

### Header Actions

| # | Element | Type | Code Reference | Notes |
|---|---------|------|----------------|-------|
| A1 | Export CSV button | button | `handleExportCSV()` | |
| A2 | Refresh button | button | `loadLogs(true)` | |

### Filter Controls

| # | Element | Type | Code Reference | Notes |
|---|---------|------|----------------|-------|
| A3 | Search input | input[type=text] | `setSearchQuery()` | |
| A4 | Action filter dropdown | select | AuditAction enum | |
| A5 | Entity Type filter dropdown | select | EntityType enum | |
| A6 | Start Date input | input[type=date] | `setStartDate()` | |
| A7 | End Date input | input[type=date] | `setEndDate()` | |

### Log List

| # | Element | Type | Code Reference | Notes |
|---|---------|------|----------------|-------|
| A8 | View Changes expandable (each) | details/summary | Shows before/after JSON | |
| A9 | Rollback button (each, conditional) | button | `handleRollbackClick(log)` | |
| A10 | Load More button | button | `handleLoadMore()` | |

### Rollback Dialog

| # | Element | Type | Code Reference | Notes |
|---|---------|------|----------------|-------|
| A11 | Confirm Rollback button | button | `confirmRollback()` | |
| A12 | Cancel button | button | Closes dialog | |

---

## NAVIGATION

### Header
// see above. Identity that was selected should show up in adition to role (User/Admin).
**File:** `components/layout/Header.tsx`

| # | Element | Type | Code Reference | Notes |
|---|---------|------|----------------|-------|
| H1 | Mobile menu toggle | button | `setMobileMenuOpen(!open)` | |
| H2 | Logout button | button | POST `/api/auth/logout` → redirect | |

### AdminSidebar
// see above. there seems to be a collapsable side bar element from before still around.
**File:** `components/layout/AdminSidebar.tsx`

| # | Element | Type | Code Reference | Notes |
|---|---------|------|----------------|-------|
| N1 | Event Setup link | Link | `/admin/setup` | |
| N2 | Team Management link | Link | `/admin/team` | |
| N3 | Shift Schedule link | Link | `/admin/shifts/schedule` | |
| N4 | Audit Log link | Link | `/admin/audit` | |
| N5 | Back to User View link | Link | `/app/calendar` | |

### UserSidebar
// as said. it propbably is worth being able to be able to navigate to the selection of identity again.
**File:** `components/layout/UserSidebar.tsx`

| # | Element | Type | Code Reference | Notes |
|---|---------|------|----------------|-------|
| N6 | Calendar link | Link | `/app/calendar` | |
| N7 | Export link | Link | `/app/export` | |
| N8 | Admin Panel link (conditional) | Link | `/admin/setup` (if isAdmin) | |

---

## SUMMARY

**Total Interactive Elements:** 100+

| Page | Element Count |
|------|---------------|
| Login | 3 |
| Identity | 6 |
| User Calendar | 17 |
| Export | 2 |
| Admin Setup | 18 |
| Admin Schedule | 26 |
| Admin Team | 18 |
| Admin Audit | 12 |
| Navigation | 8 |

---

**Add your comments in the Notes column to guide v2.1 implementation.**
